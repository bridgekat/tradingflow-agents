# Copy to `.env` (in the directory you run the agent from) and fill in.
DEEPSEEK_API_KEY=

# Optional overrides (defaults shown).
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-v4-flash
